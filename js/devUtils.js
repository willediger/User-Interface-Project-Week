//used in developing to scroll the design file and rendered page to the same pixel locations for easy comparisons down the page/design file

const scrollto = y => {window.scrollTo(0, y)};
const gety = () => window.pageYOffset;